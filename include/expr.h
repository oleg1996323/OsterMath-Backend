#include "expr_listener.h"

class Expr{
    public:

    //может хранить функцию (определяется рекурсивно в лямбде)
    //может хранить массив для конкретной переменной (например, __I1__ : [*массив данных*])
    //может хранить значение переменной (например, __K10__ : 10)
    //может хранить функцию переменной (например, __L25 : LN(10^2*5))

    private:

    
};